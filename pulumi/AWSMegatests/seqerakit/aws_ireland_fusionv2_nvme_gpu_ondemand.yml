compute-envs:
  - name: "aws_ireland_fusionv2_nvme_gpu_ondemand"
    workspace: "$ORGANIZATION_NAME/$WORKSPACE_NAME"
    credentials: "$AWS_CREDENTIALS_NAME"
    wait: "AVAILABLE"
    file-path: "./current-env-gpu-ondemand.json"
    overwrite: True
