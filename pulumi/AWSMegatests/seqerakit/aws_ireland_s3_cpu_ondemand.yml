compute-envs:
  - name: "aws_ireland_s3_cpu_ondemand"
    workspace: "$ORGANIZATION_NAME/$WORKSPACE_NAME"
    credentials: "$AWS_CREDENTIALS_NAME"
    wait: "AVAILABLE"
    file-path: "./current-env-cpu-s3-ondemand.json"
    overwrite: True
