compute-envs:
  - name: "aws_ireland_fusionv2_nvme_cpu_spot"
    workspace: "$ORGANIZATION_NAME/$WORKSPACE_NAME"
    credentials: "$AWS_CREDENTIALS_NAME"
    wait: "AVAILABLE"
    file-path: "./current-env-cpu-fusion-spot.json"
    overwrite: True
